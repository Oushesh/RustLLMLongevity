# 🧬 RustLLMLongevity

**A modern Rust backend for AI-driven longevity insights — integrating LLMs, vector search (Qdrant), and health data modeling (hair, skin, testosterone).**

This service uses **Axum** for web routing, **Prisma ORM** for database management, and **Qdrant** for semantic vector search and retrieval-augmented generation (RAG).  
Parallel data processing is powered by **Rayon** for optimal performance on CPU-intensive tasks.

---

## 📁 Project Structure

RustLLMLongevity/
├── Cargo.toml # Rust package manifest (dependencies, metadata)
├── Cargo.lock # Auto-generated lockfile for dependency versions
├── .env # Environment variables (API keys, DB URLs)
├── .gitignore # Git ignore rules
├── Dockerfile # Docker build configuration
├── docker-compose.yml # Docker Compose services (e.g., Postgres, Qdrant)
│
├── prisma/
│ ├── schema.prisma # Prisma ORM schema (models & datasource)
│ └── migrations/ # Auto-generated DB migrations
│ └── ... # (Generated by Prisma CLI)
│
├── src/
│ ├── main.rs # Entry point — lightweight bootstrap
│ ├── app.rs # Axum router, middleware, and app setup
│ ├── config.rs # Reads & manages environment configuration
│ ├── db.rs # Initializes Prisma client connection
│ ├── qdrant.rs # Vector database (Qdrant) helper utilities
│ │
│ ├── routes/ # REST / API endpoints
│ │ ├── mod.rs # Route registration
│ │ ├── health.rs # /health — app health check endpoint
│ │ ├── chat.rs # /chat — LLM + RAG chat integration
│ │ ├── search.rs # /search — semantic / vector search
│ │
│ └── utils/ # Shared utilities and helpers
│ ├── errors.rs # Centralized error types & handling
│ ├── llm.rs # Calls to LLM APIs (OpenAI, Anthropic, etc.)
│ └── embeddings.rs # Embedding creation & query logic
│
├── scripts/
│ ├── dev.sh # Dev runner (e.g., cargo watch + hot reload)
│ └── seed_data.rs # Script to seed initial DB data
│
└── README.md # Project documentation
